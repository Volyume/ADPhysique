/**
 * capability/directory/conditions.js - the disability / long-term-
 * condition knowledge directory (gap-closure order section 4).
 *
 * Every profile validates against validateConditionProfile (schema.js)
 * in the directory suite; evidence entries cite the banked research
 * (research/R5-population-evidence.md, research/R7-condition-directory-
 * evidence.md) with source, year, URL, tier and a verbatim quote.
 * Content is populated by the gap-closure Phase B adjudication; an empty
 * array is a valid (inert) state.
 */

export const CONDITION_PROFILES = Object.freeze([]);
