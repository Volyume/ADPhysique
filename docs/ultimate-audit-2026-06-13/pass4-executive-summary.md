# PASS-4 — EXECUTIVE SUMMARY

Per `_AUDIT-SPEC.md:284-288`. Anchored to `pass3-comparison-matrix.md`, `pass3-v2-founder-decisions.md`, and the
Pass-4 blueprints. The recurring lesson of this audit: **Volyume is more built than its own audit assumed** —
multiple "gaps" turned out already-implemented on read-back.

## WHERE VOLYUME LEADS (protect; do not regress)
Full deterministic adaptive loop (calories+volume+steps+cardio off one weight trend); transparent "why" coaching +
register switching; always-on tier-blind ED-safety floors; ED-safe streak-freeze; composite strength standing;
the curated bodybuilding-meal engine (roles + dry/cooked + floor-safe assembler); on-device OCR label capture
(the "AI photo logging" bar, no LLM); accessibility-forward design (tested contrast/CVD/larger-text/reduce-motion,
52px targets); hero-first glanceable Home (no AI-slop); auto-derived Fast Check-In.

## WHERE VOLYUME MATCHES
Barcode; rest timer; set-type system (exceeds Hevy's tags); deterministic mesocycle periodisation; progressive
disclosure; recap pattern (Wrapped-style, block+annual already shipped).

## WHERE VOLYUME LAGS (and the disposition)
UK curated/paid food DB — **deferred (cost)**; micronutrients/NRV — **build (Tier 3, Q1-blocked)**; passive
wearable cardio import — **build**; exercise demo media, HRV/sleep, posing, reverse-diet, challenges —
**dismissed by founder**.

## 10 HIGHEST-IMPACT CHANGES (approved)
1 Calorie banking · 2 Micronutrients/NRV · 3 Named autonomy modes · 4 Recomp-reframing view · 5 Gate train/rest
cycling · 6 Protein-consistency · 7 Analytics windows 14/30/90d · 8 Passive cardio import · 9 Auto grocery list ·
10 Plan diff/preview.

## 5 THAT MOVE A BEGINNER FROM CONFUSED TO COMMITTED
1 Gate train/rest cycling (simple flat targets, not silent carb-cycling) · 2 Recomp-reframing view (stops the
"scale flat, I quit" churn) · 3 Keyboard-completes-the-set (faster logging) · 4 Auto grocery list (turns a plan
into action) · 5 Protein-consistency (one clear, plain feedback metric).

## 5 THAT MAKE A COMPETITOR UPGRADE
1 Calorie banking ("plan a bigger day") · 2 Micronutrients/NRV depth · 3 Named autonomy modes (Coached auto-apply)
· 4 Passive cardio import · 5 Plan diff/preview.

## SINGLE BIGGEST UNTAKEN OPPORTUNITY
The **curated/verified UK food database** — the one defensible wedge every (US-built) competitor lacks, sourced +
corroborated in the research. Currently **deferred on cost** ("pay later if it takes off"); the single highest-
leverage move the moment revenue allows.

## HIGHEST-IMPACT NAVIGATION / IH CHANGE
No nav change was approved (dense/personalisation toggle dismissed; hero-first glanceability is already a lead to
protect). The nearest IH change is **timeline food logging** (Tier 3) — and only if it replaces, not clutters.

## WHAT MUST NOT BREAK
- ED-safety: floors (1,200/1,500 + FFM) and the under-eating/rapid-loss detection. **Calorie banking must preserve
  the weekly total and never push any day below a floor.**
- The deterministic engine boundary (no LLM/AI) — every approved item is deterministic.
- The locked no-jargon voice (RIR etc. stay off surfaces).
- Offline-first + EU data residency.
- The LEADS above (adaptive loop, streak-freeze, strength standing, meal engine, OCR, accessibility).

## STATUS / NEXT
Pass-4 blueprints drafted for all 19 approved items (mandated format, source-tagged, spot-verified — no
fabrication found). The exit gate is **NOT yet passed**: 66 NEEDS-ANSWER ids are open
(`pass4-needs-answer-register.md`) — most read-answerable, a defined set are FOUNDER DECISIONS (safety auto-apply,
Core-Haptics dependency, relative-framing wording, swap volume-tracking, timeline replace-vs-toggle, banking
surface), one BLOCKED on Q1 schema authority. Resolve those → re-run `pass4-final-reconciliation.md` → build per
`pass4-master-priority.md`, Tier 1 first, each with invariant tests + a fresh-eyes review.
