# CAMPAIGN 23 — FOUNDER RULINGS FOR PHASE 2 (verbatim, 2026-08-17)

Received in answer to PROGRESS-UX-SPEC.md §33. Both rulings are LOCKED.
Phase 2 implements against the spec plus these rulings; no further
routine founder questions.

---

## R1 — DERIVED SIGNAL ONLY.

> The Progress landing Visual pillar must show the useful derived
> visual-progress intelligence — assessment/progress signal, trend,
> confidence and comparison status where legitimately available — but
> no photo thumbnail. Actual imagery remains inside Progress Photos.
> The Visual pillar is a core evidence pillar, not a navigation tile.

## R2 — CONNECT THE BOUNDED CORROBORATION TO THE AUTHORITATIVE COACH.

> Do not retain D18's render-time-only split as the final
> architecture. Feed only the coarse locally-derived
> photoCorroboration contract into the authoritative runWeeklyCoach
> call.
>
> Its authority remains exactly bounded:
>
> * may only move the existing emitted confidence by the current
>   one-step corroboration rule;
> * supports-only;
> * never originates evidence;
> * never exits data-hold;
> * all existing ED/calm/safety suppression remains senior;
> * MUST NOT alter calories, macros, training, volume, floors,
>   recovery, held decisions or any other prescription.
>
> Raw photos, scan assets, scan scores, body-composition estimates,
> detailed derived measurements, scan IDs and scan history remain
> local-only and must never enter sync or coach persistence.
>
> Persist/sync only the ordinary resulting coach output required for
> cross-device consistency. Do not persist an explicit photo-derived
> input or source flag unless technically unavoidable and separately
> justified.
>
> Record the privacy-law amendment precisely:
>
> "Raw photos and scan-derived measurements remain on-device. A
> locally derived, non-reversible corroboration signal may contribute
> only to the bounded confidence of an authoritative coaching result;
> underlying visual evidence is never uploaded or synced."

---

Closing order (verbatim): "R1 and R2 are now locked. Proceed to
Campaign 23 Phase 2 against the authoritative specification with no
further routine founder questions."
