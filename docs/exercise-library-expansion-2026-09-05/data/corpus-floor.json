{
  "floor": 564,
  "note": "552 legacy RAW rows + 18 REQUIRED_EXERCISES (EL-15) - 6 EL-21 retirements = 564. The guard fails the build if CORPUS.length drops below this. Raise it when a later expansion stage lands new rows; never lower it without a founder-reviewed retirement list.",
  "cuesRequired": false,
  "demandAxesRequireReason": false,
  "demandAxesRequireReasonNote": "07-CORPUS-FORMAT.md section 6 requires an explicit unknownAxes reason for every null demand axis. The pre-existing 552+18 rows carry ~60 legitimate CAP-8 unknowns the demand-coverage report already lists (scripts/demand-coverage-report.mjs NULL worklist) but that were never individually annotated with a reason string — annotating all of them is unstarted, lead-scale content work outside this format-migration stage, not a defect in the conversion. Same phased-rollout shape as cuesRequired: false. Flip to true once that annotation pass lands; until then the guard reports null-axis counts but only hard-fails when a NEW corpus entry (added after this flag exists) carries a null axis with no unknownAxes reason."
}
