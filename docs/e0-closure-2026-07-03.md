> ⚠ STATUS (2026-07-10): SUPERSEDED/CLOSED - do not build from this document. Truth-reconciliation closure doc, already self-closed on 2026-07-03; historical record. Current work runs from docs/ux-world-class-audit-2026-07-09/_HANDOVER-AND-RESUME.md and docs/TASKBOARD.md. Pre-campaign items require the D37 triage rule before any consideration.

# E0 — Truth reconciliation: landing verification (CLOSED 2026-07-03)

The v3 programme's four-point closure checklist, verified against the repo
and completed where a point had drifted.

## 1. deploy-migrations trigger — FIXED in this closure
Found: the workflow still carried `push: branches [main], paths
supabase/migrate_*.sql` alongside workflow_dispatch. Fixed: the push trigger
is retired; the workflow is **workflow_dispatch-only**, kept as an optional
founder-run tool (the safety machinery — tracking table, <=058 baseline —
is unchanged).

## 2. CLAUDE.md statements — CORRECTED in this closure
- Migrations founder-applied manually: already stated (section 2, Database
  schema); now also states the workflow is manual-dispatch only.
- Stale count corrected: 91 -> **96** migration files.
- Outstanding-actions line corrected: 092/093/094 -> **092..099**, with the
  HELD pair referenced.

## 3. Migration status, stated explicitly
- **Applied through 091** per supabase/README (048/050-055/058 confirmed
  2026-06-01 set; see README lines 22-31).
- **092..099 pending founder apply** (partner end purge, landmark
  telemetry, users_profile sex, trial resume window, delete-user
  completeness 2, deletion log anonymise, deletion sweeper, funnel
  telemetry). 099 should land before the funnel build ships.
- **HELD: 049** (drop peak_week_plans) until the next AAB ships, so the
  frozen closed-test build keeps its table. **HELD: 059** (numbered meal
  slots) is "pending founder apply — apply before a client writing meal_N
  slots reaches production sync" (supabase/README lines 60, 74-81).

## 4. Engine golden-coverage statement (honest, current)
Deterministic-engine test surface at this HEAD (suite counts by module):
planEngine 12, weeklyCoach 7, coachApply 2, mesocycle 2, nutritionEngine 1,
coachingGoals 1, planAutoGen 1, poolGenerator 1, cardioEngine 1,
edPatternDetector 1, wellbeing 1 — plus the two replay baselines
(`adaptiveTdee.b1.replay`, `phaseVocab.en4.replay`) that pin engine outputs
byte-for-byte across the corpus, and the ED-floor invariant suites that run
against the REAL engine.

Named untested/thin cells, honestly:
- **nutritionEngine** carries ONE dedicated suite for the module that owns
  BMR/TDEE/macros/floors; the floors themselves are pinned by invariant
  suites, but the goal x experience x bodyFatSource matrix is exercised
  through weeklyCoach/replay fixtures rather than direct cell-by-cell
  golden tests. Direct cells NOT individually golden-pinned: the surplus
  experience multipliers per goal, Katch-McArdle vs Mifflin parity at each
  goal, and the protein-approach variants (standard/optimised/advanced)
  outside the fixture corpus.
- **cardioEngine** and **poolGenerator** have one suite each; their
  interaction (cardio load feeding weekly volume decisions) is covered only
  through weeklyCoach integration fixtures.
- The sync regression matrix documents T7 (two-device) and T8
  (offline-collision) as out of scope — a sync gap, not an engine gap, but
  recorded here because the original E0 statement bundled them.

Standing rule unchanged: engine outputs for identical inputs are pinned by
the replay corpus; any engine-adjacent change must re-run the replays and
show a byte-identical (or founder-approved delta) result.
