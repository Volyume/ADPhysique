# CC25 EXTERNAL CONSULTATION QUEUE (Amendment §20-22)

Grok/Gemini are not reachable from this environment; per §22 the exact
prompts are queued here for the founder to run at will. Responses get
reconciled into DECISION-REGISTER.md (ACCEPT/MODIFY/REJECT/DEFER each
consequential suggestion). Nothing below blocks work.

## CHECKPOINT A (now) — attack the provisional architecture

Attach: ARCHITECTURE.md + DECISION-REGISTER.md.

PROMPT A-GROK (adversarial product):
"You are red-teaming a fitness app's capability/disability architecture
(attached). Attack, with concrete user stories, not generalities:
(1) ways this still treats non-disabled training as the default;
(2) places the UX would feel patronising, othering, or surveillance-y
to a disabled strength athlete; (3) real-world capability situations
the constraint model cannot express (give the exact sentence a user
would say that has no home); (4) how a serious seated lifter's
experience could end up second-class despite CAP-1/CAP-2; (5) market/
community failure modes if this ships as described. Rank your ten
strongest attacks; no fixes, attacks only."

PROMPT A-GEMINI (systems/completeness):
"Review the attached architecture for structural holes: (1) does the
nine-axis demand ontology under- or over-fit — name capability
statements it cannot encode and axes with no consumer; (2) find
learning consumers or data flows the eligibility matrix misses (the
repo map lists ~186 readers; the matrix has ~26 rows — what class is
absent?); (3) stress the sync/erasure design: construct a timeline
where history becomes uninterpretable or erasure leaves a health
inference recoverable; (4) check the precedence table for
non-determinism or rule pairs that can deadlock a slot; (5) evaluate
CC-D9's 25% threshold: what behaviour cliff does it create and what
would you key it on instead? Number your findings; evidence-shaped,
no prose padding."

## CHECKPOINT B (after CC27's ontology/coverage work) — ontology + coverage
Queued: prompts to be drafted with the coverage report attached
(hidden able-bodied assumptions per axis; disability groups the
ontology fails; over-engineered axes).

## CHECKPOINT C (before the routine-family roadmap freezes in CC28)
Queued: missing populations/families; duplicate families; groups that
must stay capability-led; unsafe condition-specific overreach.

## CHECKPOINT D (final red team before the workstream's marketing gate)
Queued: full-architecture break attempt with the validation results
attached.
