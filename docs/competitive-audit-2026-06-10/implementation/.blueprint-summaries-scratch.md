# Round-2 blueprint summaries scratch (orchestrator working notes for the master roadmap)

## COMP-012 Trust row (COMPLETE)
PLACEMENT: one muted non-interactive line on Welcome BELOW both tier cards, above "Already have an account?" (claims apply to both tiers; above would push Pro card down). Store listings get "YOUR DATA, PLAINLY" block; paywall one footer line shared w/ COMP-007; learn-more reuses SettingsData/Privacy. Zero new surfaces.
BEST REF: MacroFactor (claim + mechanism: "premium app... we don't need to" sell data/ads). Volyume adds what MF can't: fully offline food logging.
LOAD-BEARING: (1) DROP "no trackers" - Sentry is classified a tracker by Exodus (profile 447), one APK scan falsifies it. Ship "Works fully offline · Exports anytime · No ads, ever" (never-sold is locked policy). (2) Lead listings with offline FOOD search (lifting offline = Hevy parity; food = category outlier). (3) One-trust-row rule on Welcome (COMP-006 also wants a line - dedupe; trim FREE_BULLETS[0] duplicate).
FAILURE RISK: claim drift - new SDK silently invalidates copy; add re-run-claim-table step to sub-processor procedure. FLAGGED (not fixed): iOS privacy label wrongly says crash data "not collected"; PRIVACY_CONSENT_LOCKED names RevenueCat which isn't in build.
EFFORT: holds at 1 (~half day). Paywall line is billing-adjacent -> founder "proceed" needed per CLAUDE.md.

## COMP-022 Barcode-miss chain (COMPLETE)
KEY CODE FINDING: chain already ~80% wired - miss routes ScanBarcode->ScanLabel w/ prefillBarcode (ScanBarcodeScreen.js:121-125); OCR->AddCustomFood prefill exists; barcode_ean persists (AddCustomFoodScreen.js:83); findLocalByBarcode resolves custom-first. COMP-022 = experience layer + 4 plumbing defects.
PLACEMENT: no new screen. Miss = arrival choice state on ScanLabelScreen (camera warm behind scrim, card: "Not in the database yet... fix it once"; "Scan the label" primary ~30s honest estimate; "Type it in" secondary + persistent type-in escape during capture). Success = toast in AddCustomFood: "Saved. Next time this barcode scans instantly."
BEST REF: Cronometer capture chain. Volyume beats: healed barcode on-device (instant/offline), healing explicitly told, contributions to OFF commons.
LOAD-BEARING: (1) confirm-the-healing toast (failure memory -> investment memory); (2) move OFF write-back from capture to save (currently queues unconfirmed OCR pre-confirm, contradicting consent copy; also zero partial state on abandon; consent asked once post-first-chain via dismissible Diary card); (3) honest miss vs unreachable (waterfall collapses offline timeout + true miss into one null - tag result, offline copy "label scanning still works offline" true since MLKit on-device).
FAILURE RISK: the ~30s promise vs real UK label OCR quality (Foodvisor's failure mode: overpromised capture). Mitigate: type-in always visible; monitor prefillConfidence before strengthening copy.
EFFORT: 2 CONFIRMED (days). No migrations, no new deps. [SE] flags on Cronometer/Yuka cites.

## COMP-023 Day-3 trial moment (COMPLETE)
PLACEMENT: one local push at trial start+3d 10:00 (quiet-hours shifted, cascade-gate scheduler pattern) + Home line at SECOND priority in existing single-banner chain (coach > trial countdown > deload > phase). Deep links to existing surfaces (check-in gate screens for has-data; session hero for has-nothing). Day 7 in-app only. Total trial pushes: day 3 (value), 12 (warning), 14 (outcome).
BEST REF: Blinkist trial transparency (+23% trial starts, -55% complaints, push opt-in 6%->74%) run value-side: personally-dated data-driven countdown, not charge reminder.
LOAD-BEARING: (1) variant selected at schedule time + re-laid on every app open days 0-3; (2) unlock-date helper shares literal gate constants (FIRST_CHECKIN_MIN_DAYS=5, MIN_WEIGH_INS=3) so promised date can't drift from gate maths; (3) restraint as design: exactly one new push/trial, ED-flag suppression of weight asks, zero sales language.
FAILURE RISK: broken first promise (says "unlocks Sunday", gate refuses). Mitigated by single-source constants + gate-parity fixture tests.
EFFORT: 2-2.5 (approved 2). No DB, no billing files.
!! PRE-EXISTING BUG FOUND (mention, not fixed): restoreNotifications (scheduler.js:451-471, called RootNavigator.js:690) wipes ALL scheduled notifications then re-lays only morning+check-in -> the day-12/14 CASCADE GATE pushes laid at startCascade are DESTROYED on next launch and never fire. Revenue-relevant. Blueprint recommends founder-approved cascade re-lay in same PR. Also: NOTIF_ID_CASCADE_19/21 legacy-named but correctly fire day 12/14 - don't "correct" them.

## COMP-024 Cycle-robust smoothing (COMPLETE)
ALGORITHM: asymmetric Huber-clamped robust-innovation EWMA. Alpha stays 0.1; clamp daily innovation at k*s (k~1.5, s = MAD of recent innovations, 0.25kg floor). ASYMMETRIC: tight on upward (water-gain) innovations, wide/disabled on downward = safety guarantee encoded in the maths. (B median prefilter, C dual-EWMA documented, not recommended.)
BEST REF: MacroFactor Expenditure V3 (Oct 2024) - robust to fluid retention "without period tracking"; framed as stability/responsiveness frontier, NOT a cycle feature. Maths: Duran-Martin robust EWMA + Huber; physiology Kanellakis 2023 (+0.45kg at menses mean, 1-3kg range).
LOAD-BEARING: (1) UNIVERSAL scope not bioSex-conditioned (noise hits everyone; sidesteps special-category inference; the MacroFactor anti-creepiness move); (2) keep both layers (smoothing fixes the trend READING; manual cycleOverride stays the explicit DECISION hold; seam documented); (3) safety reads a less-damped series (rapidLossOverride/edPattern read un-damped; if trends disagree on -1.5%, safety wins).
FAILURE RISK: masking genuine rapid loss -> mitigated by asymmetric clamp + safety-series separation + blocking fixture (-1.8%/wk must still fire). Founder maths gate + shadow mode before default flip.
EFFORT: 3 holds. Pure functions; real work = 8-fixture suite + shadow-mode telemetry (numeric only).
CAVEAT: MF/physiology cites are search-extract [SE]; re-verify before founder review.
