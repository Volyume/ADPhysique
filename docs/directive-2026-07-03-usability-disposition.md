# FOUNDER DIRECTIVE — world-class usability audit disposition (2026-07-03)

Recorded the moment received. Governing lens unchanged: product, not launch.
Nothing goes near production/store/listing/growth; iOS ships only when both
platforms are ready, on the founder's call, not yet made. Present each
wave's plan and WAIT for GO. State model tier per session. Before building
any item, a FAST subagent re-verifies the audit's cited lines (the audit is
a day old; never implement against a stale reference).

Adopted spine (the audit's three sentences): the gap is memory, not
transparency; the app under-tells its own story; the first week has trust
leaks in big moments. PROTECT list (regression-test, never touch): ED/safety
machinery and copy verbatim; streak model mechanics; insufficient-data
receipts; 14-day trial length; calm notification register; one-amber rule;
free on-ramp. No AI, no feed, no punitive mechanics, no free/Pro change.

## APPROVED
- WAVE A: Tier 0 groups A+B+C (~25 S items), one guard-tested wave, one PR.
  Non-negotiables: A1 false first-set PR (celebration layer only, engine
  untouched and VERIFIED); A2 first-food-log toast+Undo; B1 GoalLockConsent
  locked-copy regression (STRONG, correctness not polish); B2 fat-free mass
  -> lean mass + methodology deep-link; B3 "How Precision Coaching works"
  links + 14-day arc sentence. FAST verification+assembly; STRONG owns
  consent/safety/methodology copy + hostile diff review. Device checklist:
  first-set relabel, consent sentences verbatim from locked doc, no
  safety/streak/engine movement.
- S6 (lands with Wave A): churn-predictor instrumentation — sessions in
  first 14 days (research: <3 = 3-4x churn) on existing telemetry rails,
  allow-listed, opt-out honoured, behaviour only, NO health values; adopt
  the 90-second install-to-first-set benchmark as a standing test. STRONG
  review that the event carries behaviour only.
- WAVE B: S1 coach memory (outcome loop + scorecard + pre-commitment line;
  STRONG-led; scorecard suppressed under ED flag/calm via the EXISTING
  suppression pattern; FAST maps persisted per-week fields first; plan to
  founder first) + S2 forgiveness-story surfacing (Home echo, dismiss-once
  promise explainer, longest-run share card; FAST build, STRONG review of
  ED-flag behaviour on the run number; D4 question attached, ask not fix).
- WAVE C: S3 daily brief + mid-week runway (STRONG; runway MUST reuse
  coachLedger thresholds, never re-derive; FAST confirms single source
  first; plan first) + S5 plan-authoring spine (FAST stepper port +
  duplicate day; STRONG owns the giant-set integrity fix — cap at pairs now
  — and verifies no existing saved routine breaks; guard-test the
  builder<->session contract).
- WAVE D: S4 weekly mini-story + share extensions (FAST reusing StoryCard
  infra + share-card privacy field-list; STRONG verifies the field-list
  holds on new card types) + Tier 2 in ranked order: T3 navigateCrossTab
  EARLY (latent F4 bug class, correctness refactor + guard test), T1 saved
  meals into relog pool, T2 coach-change tab badge, T4 RPE/RIR progressive
  disclosure, T5-T9 as capacity allows (T7 drag-reorder only after
  confirming gesture-handler already in tree; no new dep without founder).

## FOUNDER DECISION PACK (present early, do not act): D1 Home food
quick-log options (no silent re-add; founder removed the cell 2026-06-30);
D2 intent modal options with coaching-input implications framed (feeds
readiness/sessionAdjustments); D3 Home banner consolidation design call;
D4 unlabelled missed glyph — deliberate or gap; D5 rest-day framing rule
confirmation; D7 GoalChangeSummary ED-flag asymmetry — surfaced for ED-owner
review, no fix proposed.

## SEPARATE CORRECTNESS PASS
D8 CoachReviewScreen free-tier derivation drift (weeksSinceLastDeload=99
hardcoded; jointPain boolean/numeric mismatch): own scoped session, own test
plan, STRONG. Not part of the UX waves.

## HELD / GATED
D6 exercise demo media: parked (cost+licensing). D9 quiz-first flag stays
OFF (A/B value is conversion = launch-adjacent; HOLD). D10 What's New
currency: release-checklist process note only.

## SEQUENCE
1 Wave A + S6 (D1-D7 pack in parallel, early). 2 T3 early. 3 Wave B.
4 Wave C. 5 Wave D. 6 D8 independently. Background engineering (E12 steps,
bundle cut 4, A1 UK food hit-rate) fills windows, never concurrent with the
in-flight E1 programme.
