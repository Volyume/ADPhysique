# 09 — Final adversarial journey pass (settled product)

One pass, run as three read-only persona walks against the settled tree
after all remediation lanes had landed. Each walk followed the code path a
tap takes and quoted the copy the person sees. Detail:

- `09a-FINAL-PASS-standard.md` — A new standard user, B experienced
  bodybuilder, G long-term returning user.
- `09b-FINAL-PASS-styles.md` — C kettlebell user (onboarding route and
  library route), D circuit user, plus the no-overhead substitute case.
- `09c-FINAL-PASS-limitations.md` — E stable physical requirement, F
  temporary limitation, Part 15 discoverability, Part 43 comprehension
  proof over 17 summary fixtures.

## Stop-ship-class findings from the pass, and what was done

| Walk | Finding | Fix (landed) |
|---|---|---|
| A | Three strings still called the food diary "Eat", a surface that no longer exists (check-in gate, Methodology twice) | "the food diary" |
| C | The bell ladder never fired in the live logger: the evidence packet's exercise whitelist omitted the equipment category, so a 16 kg bell prefilled 16.75 kg | Packet carries `equipmentCategory`; ladder ruled to 4 kg steps above 12 kg (16 -> 20) |
| C/D | A "no overhead" limitation on a shoulder press was answered with another shoulder press: Seated Dumbbell Press, Z-Press, Log Press and every kettlebell press derived no overhead position | Any press with a front- or side-delt primary derives overhead; metadata rederive key v6 |
| C | "Browse kettlebell plans" on Update goal and phase was a dead route (Coach stack navigating to a Train-stack screen) | Cross-tab navigation |
| C | The post-workout summary still said "add a couple of sets" for a muscle whose week held excluded explosive work | Advice withheld for that muscle; the count line says explosive lifts are not counted |

## P2 notes acted on

- Populated summary: a temporary limitation the line cannot name now says how many limitations it covers; the long-term tail after an episode says what it counts; the wizard's closing line claims exactly what the consumers do.
- Onboarding equipment answers use the same sentence case as the two other screens that ask.
- Today names a circuit day as a circuit ("Circuit · 3 stations · 3 rounds · 90s between rounds") instead of "6 exercises".

## P2 notes recorded, deliberately unchanged

- Today shows the block-complete line and the block-complete hero together. The line's rank deliberately suppresses junior lines (calorie changes, check-in prompts) during the block decision; removing it would let those through. Left as the founder-era attention rule.
- The Settings row keeps its static coverage subtitle while Coach and Train carry the live line: Settings is the door, the other two are the status.
- Kettlebells and Bands are stored as the home-gym and bodyweight generation profiles; both re-asking screens are style-locked for those plans, so the stored value is never shown back as a wrong answer.
- Train's week-complete line names Monday without the date Today shows.

## Verdicts

- A, B, G: no stop-ship anomaly after the "Eat" fix. Claim-vs-code, dead ends, wrong-state routing, silent mutation, raw errors, contradictory states, stale commercial copy, unrecoverable interruption and ED-safety suppressions all walked clean.
- C, D: clean after the four fixes above. Activation carries circuit structure and tags; round arithmetic is shared across chip, orientation, logged rows and lock screen; evidence class is stamped end to end and the consumer split holds; style-pool swaps and both rebuild screens' notices behave.
- E, F: no stop-ship anomaly. Discoverability passes in both directions; every populated line is answerable and none says "things"; baseline rules shape plans, pickers and sessions and no copy claims they shape coaching.
