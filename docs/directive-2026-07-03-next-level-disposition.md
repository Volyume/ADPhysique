# FOUNDER DIRECTIVE — next-level proposal disposition (2026-07-03)

Recorded verbatim in substance the moment received. Governing lens: make the
PRODUCT the best it can be before production on either platform. Anything
whose value is distribution/store/launch/growth is out of scope until the
founder declares readiness — that declaration has NOT been made. No approved
item starts building until its plan is presented and the founder says GO.

## Model tiering (standing rule for this programme)
STRONG model: planning, architecture, engine/coachApply-floor-adjacent,
Article 9/consent/ED-safety paths, final diff self-review. FAST subagents
(explicit sonnet/haiku per the 2026-07-02 tier rule): research, gathering,
measurement, hit-rate counting, mechanical refactors, inventories, test
scaffolding. "Find, count, measure, list" = FAST; "decide, design, change
engine/safety/consent behaviour" = STRONG. State tier at the top of every
session.

## APPROVED
- A1 (was P9) UK food-data coverage push — HIGHEST PRIORITY. Fan out FAST
  agents: top ~500 UK supermarket products test set, measure hit-rate vs the
  bundled OFF UK + CoFID corpus (exact/partial/miss, itemised misses) →
  /audit/a1-uk-food-hitrate.md. STRONG designs the coverage extension
  (provenance, licences, FTS rebuild path) and presents plan + number before
  building. The hit-rate number is recorded now; publishing it is a LAUNCH
  decision, not this work.
- A2 (was P10) rest-day surface — completeness, not "retention". New
  notification category = NOTIFICATIONS_LOCKED deviation: STRONG presents
  category/default/copy/insight source/quiet-hours+budget/ED suppression and
  STOPS for the founder's recorded decision. On approval, build per existing
  architecture; show combined weekly cadence with training reminders active.
- A3 (was P13) E12 next step — immutable families, tests first. STRONG for
  migration plan + conflict-policy parity; FAST inventories existing test
  coverage per sync path. One table family, full gate, founder approves plan
  first. Background fill; NEVER concurrent with the in-flight E1 programme.
- A4 (was P14) bundle cut 4 — FAST does the asset conversion (formTips/
  seedRoutines/seedExercises → .dat); STRONG verifies first-run seeding
  untouched. Ships with a clean-install device checklist the founder runs.
- A5 (was P15) quick-win basket — mostly FAST: getCoachOutputHistory
  deleted_at filter; RootNavigator deprecated `lazy` → screenOptions; verify
  PR-5 first-install food-import batching; Play-vitals cadence into the
  release checklist. Founder runs the on-device perf measurements himself —
  deliver him the exact scripted steps early (sequencing note). STRONG only
  for the final behavioural review.
- A6 (was P7) Wear OS 7 scoping memo — the directive's text for this item
  was truncated mid-sentence and its heading sat inside the REJECTED block,
  but the Sequencing section says "A6 whenever convenient — it's cheap
  research, no urgency". READING TAKEN (flagged to founder for correction if
  wrong): A6 is APPROVED as a research MEMO ONLY, no build.

## REJECTED (do not action; reasons = distribution/out of scope)
- P1, P2, P4 store listing headline / ASO copy / apply listing.
- P5 iOS production release — explicitly rejected; iOS ships only when the
  app is fully ready for BOTH platforms simultaneously. Do not prepare
  submission assets; do not treat as pending.
- P6 Android widget family — parked with all distribution work.
- P8 year-in-review / shareable stats — growth mechanic and Q4-seasonal;
  doubly out of scope.
- P12 coach partnerships — distribution; depends on a launch not happening.

## HELD
- P3 ED-safety as marketing copy — HOLD entirely. Most sensitive move in the
  document (risks attracting vulnerable users; medical-device questions).
  Internal architecture unchanged; no copy ships in any form without
  qualified review of exact wording, and that is launch-time regardless.
- P11 exercise media — parked; blocker is cost, not readiness. The
  "license a set" option is captured here as part of the parked note, not
  actioned.

## Sequencing
A1 first. A5's perf-baseline steps to the founder early ("before" numbers).
A2 decision pack presented early so it never blocks. A3/A4 fill windows,
never concurrent with E1. A6 whenever convenient. Nothing goes near
production; any drift toward "let's get it listed" stops and is flagged —
that decision is the founder's and has not been made.
