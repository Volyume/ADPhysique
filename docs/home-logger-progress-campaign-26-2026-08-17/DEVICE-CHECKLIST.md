# CAMPAIGN 26 — FOUNDER DEVICE-ORDER BATCH: DEVICE CHECKLIST (physical Android, EAS build)

Covers the 2026-08-17 device orders across Today, the logger and
Progress, plus the three same-day fixes already merged (tonnage landmark
gone, NowCard accent gone, Diary macros-guide row gone). Rulings: D102.

## Today (Home)

1. Open Today. Expected: header says "Today" only - no "Afternoon,
   Alex." greeting line; content starts higher.
2. Workout hero chip (Pro, active block, ordinary week). Expected:
   "On track for this block." - never "Stop N short of failure".
   Recovery/deload/readiness caution lines unchanged when those states
   are live. Tap still opens the block-shape sheet.
3. Before logging weight (Pro): the morning-weight strip renders as the
   action row. Below it, the quiet evidence pane: title "Since your
   check-in" once ANY check-in exists in history ("What your coach is
   reading" only for an account that has never checked in - the pane
   NEVER says "first review" in any state), "N days to your next
   check-in", weigh-in row, sessions row. On an account with weeks of
   check-ins the title must read "Since your check-in" even mid-week
   before this week's check-in.
4. Log the weight. Expected: the strip DISAPPEARS entirely - no
   bordered card, no green Logged pill. The pane now carries a quiet
   "Morning weight <value>" tick row. Whole pane taps through to the
   You readiness surface.
5. Weigh-in counts: with 2 weigh-ins this week the row reads "2 of 3
   morning weigh-ins this week"; log a 4th and it reads "4 morning
   weigh-ins this week" (actual count, never "3 of 3").
6. ED-safety (if a test flag state is available): under calm mode/open
   flag the pane shows only its neutral title and the countdown - no
   counts, no weight line; the weigh-in strip behaviour is unchanged
   from before this campaign.
7. Check-in deep link "Log my weight first" still opens the strip's
   input (unlogged day).

## Logger (Active workout)

8. Open a session. Exercise header: name and three muted dots on ONE
   row, no rounded-square box around the dots; dots dim while pressed;
   tap target still comfortable (44dp). Overflow sheet unchanged.
9. The session header shows NO "This week: stop N short of failure"
   line, any week of the block.
10. The active set card shows NO coach sentence ("Down a little today.
    Steady here." etc.) on any working set - just position line,
    prefill, steppers, note row. Warm-up sets keep their warm-up label;
    superset group flashes still appear.
11. The prescription numbers still adjust as before (readiness trim,
    session adjustment) - only the standing explanation is gone. The
    adjustment sheet (target row) still shows the written why on
    demand.
12. Log several sets; PR/record line, rest timer, Finish flow all
    unchanged.

## Progress

13. Progress landing top boxes: no text cut off mid-sentence anywhere
    in the three pillar rows (Training / Body / Progress photos) - long
    lines wrap to extra lines instead.
14. Regression from the earlier same-day batch: third pillar is
    labelled "Progress photos"; no tonnage milestone row anywhere; no
    share CTA on the landing.

## Eat (Diary)

15. Regression: no "New to macros? Read the 5-minute guide" row under
    the rings. The guide itself still opens from Nutrition targets and
    the Pro setup-complete screen.
