// Stubs static asset imports (png, jpg, fonts, etc.) for jest. Returns
// a numeric placeholder ID so RN's require-based asset resolution sees
// "something" without trying to actually load the binary.
module.exports = 1;
